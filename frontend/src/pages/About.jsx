import { ArrowDown, ArrowRight, BrainCircuit, Code2, Database, Server } from 'lucide-react'
import { Link } from 'react-router-dom'

const steps = ['Student Inputs', 'FastAPI API', 'Machine Learning Model', 'Predicted Score', 'Performance Insights']

export default function About() { return <main className="page-shell"><section className="max-w-3xl"><p className="eyebrow">About the project</p><h1 className="mt-2 text-4xl font-semibold tracking-tight text-ink">A focused interface for a machine learning prediction service.</h1><p className="mt-5 text-lg leading-8 text-slate-600">EduPredict AI is a machine learning based student performance prediction system. It turns a small set of student inputs into a prediction request and presents the server’s response clearly.</p></section><section className="mt-12 grid gap-5 lg:grid-cols-2"><article className="card p-6 sm:p-7"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-50 text-brand-700"><Code2 size={20} /></span><h2 className="mt-5 text-xl font-semibold text-ink">Technology</h2><div className="mt-5 flex flex-wrap gap-2">{['React', 'FastAPI', 'Python', 'Pandas', 'Scikit-learn', 'Machine Learning'].map((item) => <span key={item} className="rounded-lg border border-slate-200 bg-slate-50 px-3 py-1.5 text-sm font-medium text-slate-700">{item}</span>)}</div></article><article className="card p-6 sm:p-7"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-50 text-brand-700"><BrainCircuit size={20} /></span><h2 className="mt-5 text-xl font-semibold text-ink">What this frontend does</h2><p className="mt-3 text-sm leading-6 text-slate-600">This React app validates student inputs, calls the FastAPI REST API through Axios, and visualizes only the submitted factors and returned prediction. It does not train models or access datasets directly.</p></article></section><section className="mt-10 card p-6 sm:p-8"><p className="eyebrow">Workflow</p><h2 className="mt-2 text-2xl font-semibold text-ink">From input to insight</h2><div className="mt-8 flex flex-col items-center gap-3 lg:flex-row lg:justify-between">{steps.map((step, index) => <div className="contents" key={step}><div className="w-full rounded-xl border border-slate-200 bg-slate-50 p-4 text-center text-sm font-semibold text-slate-700 lg:w-40">{step}</div>{index < steps.length - 1 && <ArrowRight className="hidden shrink-0 text-slate-300 lg:block" size={18} aria-hidden="true" />}{index < steps.length - 1 && <ArrowDown className="text-slate-300 lg:hidden" size={18} aria-hidden="true" />}</div>)}</div></section><section className="mt-10 rounded-2xl border border-brand-100 bg-brand-50 p-7 sm:flex sm:items-center sm:justify-between"><div><h2 className="text-xl font-semibold text-ink">Ready to explore a prediction?</h2><p className="mt-2 text-sm text-slate-600">Use the prediction workspace to send current student factors to the API.</p></div><Link to="/predict" className="button-primary mt-5 sm:mt-0">Predict Score <ArrowRight size={17} /></Link></section></main> }
